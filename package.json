{
  "name": "menugo-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "node scripts/dev.js",
    "dev:backend": "npm --prefix menugo-backends run dev",
    "dev:frontend": "npm --prefix menugo-frontend run dev"
  }
}